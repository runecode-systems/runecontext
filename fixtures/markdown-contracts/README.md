# Markdown Contract Fixtures

These fixtures exercise the strict markdown parsing rules for `proposal.md` and `standards.md`.

- `valid-*` fixtures should parse and validate successfully.
- `reject-*` fixtures should fail with a deterministic diagnostic.
- These fixtures cover structure only; richer workflow behavior remains in later milestones.
