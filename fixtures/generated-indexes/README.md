# Generated Index Fixtures

These fixtures cover alpha.4 Branch Cut 4 generated manifest/index output.

- `golden/traceability-manifest.yaml`: manifest output from
  `fixtures/traceability/valid-project`.
- `golden/traceability-changes-by-status.yaml`: lifecycle-grouped change index
  output from `fixtures/traceability/valid-project`.
- `golden/bundle-resolution-bundles.yaml`: bundle inventory output from
  `fixtures/bundle-resolution/valid-project`, including direct parents,
  resolved-parent ordering, and referenced include/exclude patterns.

All generated-index goldens are expected to remain optional/regenerable and
deterministically ordered.
