# Claude Code Flow: change assess-intake

Map conversational intake assessment to:

```sh
runectx change assess-intake --title "<title>" --type <type> [--size <size>] [--bundle <bundle-id>] [--description "<text>"] [--path <project-root>]
```
