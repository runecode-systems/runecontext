# Tasks

- Define the default history-preview behavior for closed and superseded sections while always showing all active work.
- Add `--history recent|all|none`, `--history-limit N`, and `--verbose` semantics for human status output.
- Emit clear hidden-history hints and ensure zero or short historical sections remain clean and non-erroring.
- Add CLI tests for bounded previews, full-history expansion, suppressed-history views, and machine-contract preservation.
