# Verification

## Notes
Run ./bin/runectx validate --path . and just test. Before close, verify update and close coverage for verified-state transitions, recursive umbrella propagation, non-recursive defaults, and rollback on partial invalid cascades.

## Planned Checks
- `just test`

## Close Gate
Use the repository's standard verification flow before closing this change.
