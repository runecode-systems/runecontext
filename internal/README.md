# Internal

This directory holds shared Go packages for RuneContext implementation code.

## Current Packages

- `internal/contracts/`
  - executable contract-validation foundation for schemas, markdown structure, YAML profile rules, and alpha.1 traceability checks
